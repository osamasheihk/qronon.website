# Qronon Website

Next.js + TypeScript marketing site for Qronon.

## Commands

- `npm run dev` - local development server
- `npm run build` - static export build

The app uses Next.js static export so it can be deployed to static hosting. Legacy `.html` URLs are handled by redirect files in `public/`.
